import InnerPage from '../components/InnerPage';

export default function Reps() {
  return (
    <InnerPage
      label="Field operatives"
      title="The Reps."
      paragraphs={[
        'Our representatives are the vanguard of the BOTCORE network — hybrid operators trained to translate deep technical architecture into the language of stakeholders, clients, and creative partners.',
        'Each rep is embedded into a vertical: fintech, biotech, aerospace, or consumer synthetic media. They act as the primary conduit between mission control and the field.',
        'Engage a rep to initiate a scoped discovery call. Zero friction, maximum signal.',
      ]}
    />
  );
}
